[project]
name = "session5cli"
version = "0.1.0"
description = "Um gerenciador de tarefas modular e intuitivo construído com Python, Typer e JSON"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "typer>=0.24.1",
]
